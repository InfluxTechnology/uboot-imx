config INF_NO_UART_FLUSH
	bool "Disable flush of UART at boot"
	default n

config INF_IMX_PTP
	bool "Enable when using Influx Technology test platform"
	default n
	help
	  Enable this option when building a u-boot for the Influx Technology
	  test platform

